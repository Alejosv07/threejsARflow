import { defineConfig } from 'vite';

export default defineConfig({
  // **Esta línea es CRUCIAL para GitHub Pages.**
  // Debe ser el nombre exacto de tu repositorio, precedido y seguido de un slash.
  base: '/threejsARflow/', 
  
  plugins: [],
});